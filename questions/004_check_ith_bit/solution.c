#include <stdint.h>

/**
 * @brief Check if the i-th bit of a 32-bit unsigned integer is set.
 *
 * @param num   The input 32-bit unsigned integer.
 * @param i     The bit position to check (0 = LSB, 31 = MSB).
 * @return      1 if the i-th bit is set, 0 if it is clear.
 */
int check_bit(uint32_t num, int i) {
    /* TODO: Implement your solution here */
    return 0;
}
