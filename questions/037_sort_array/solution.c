#include <stdint.h>

/**
 * @brief Sort an array of integers in ascending order, in-place.
 *
 * Requirements:
 * - O(1) auxiliary space (no malloc, no extra arrays)
 * - Recursion discouraged (embedded stack constraints)
 * - Must handle negative values, duplicates, INT_MIN, INT_MAX
 *
 * Recommended approaches:
 * - Insertion sort: simple, stable, O(1) space, O(n) for nearly-sorted
 * - Shell sort: O(1) space, sub-quadratic, iterative
 * - Heapsort: O(n log n) worst-case guaranteed, O(1) space
 *
 * @param arr  Pointer to the array to sort in-place
 * @param n    Number of elements in the array
 */
void sort_array(int32_t *arr, int n) {
    /* TODO: Implement your solution here */
    (void)arr;
    (void)n;
}
