#include <stdint.h>

/**
 * @brief Determine whether the system is little-endian or big-endian at runtime.
 *
 * Store a known multi-byte value and inspect the byte at the lowest address.
 *
 * @return  1 if the system is little-endian, 0 if big-endian
 */
int is_little_endian(void) {
    /* TODO: Implement your solution here */
    return 0;
}
