#include <stdint.h>

/**
 * @brief Swap the byte order (endianness) of a 32-bit unsigned integer.
 *
 * @param num   The input 32-bit unsigned integer.
 * @return      The value with bytes in reversed order.
 *
 * @note Also consider: parallel bit manipulation (SWAR) approach for O(1) branchless solution.
 */
uint32_t swap_endianness(uint32_t num) {
    /* TODO: Implement your solution here */
    return 0;
}
