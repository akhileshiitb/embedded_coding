#include <stdint.h>

/**
 * @brief Find the position of the first (least significant) set bit.
 *
 * @param num   The input 32-bit unsigned integer.
 * @return      Position of the first set bit (1-indexed), or 0 if num is zero.
 *
 * @note Also consider: parallel bit manipulation (SWAR) approach for O(1) branchless solution.
 */
int find_first_set(uint32_t num) {
    /* TODO: Implement your solution here */
    return 0;
}
