{
  "id": 39,
  "title": "UART Driver with Generic Interface",
  "slug": "uart_driver",
  "difficulty": "hard",
  "tags": ["driver", "uart", "interface-design", "vtable", "register-access", "bare-metal", "volatile"],
  "function_name": "uart_ops (vtable)",
  "parameters": [
    {"name": "handle", "type": "void*", "description": "Opaque pointer to uart_device"},
    {"name": "config", "type": "const void*", "description": "Pointer to uart_config struct"},
    {"name": "data", "type": "uint8_t*", "description": "Data buffer for read/write"},
    {"name": "len", "type": "size_t", "description": "Number of bytes to read/write"}
  ],
  "return_type": "int (error code)",
  "constraints": [
    "Must access registers via volatile pointer (handle->regs)",
    "Must validate all parameters and return error codes",
    "Must enforce lifecycle: init before open, open before read/write",
    "System clock is 16 MHz",
    "Polling only — no interrupts"
  ]
}
