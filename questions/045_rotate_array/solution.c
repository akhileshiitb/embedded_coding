#include <stdint.h>

/**
 * @brief Rotate an array to the left by k positions, in-place.
 *
 * Elements shift toward the front; those that fall off wrap to the back.
 * Must use O(1) auxiliary space (no full-size temporary array).
 * Remember to normalize k (k may be >= n).
 *
 * @param arr  Pointer to the array
 * @param n    Number of elements
 * @param k    Number of positions to rotate left
 */

void rotate_left(int32_t *arr, int n, int k) {
    /* TODO: Implement your solution here */
}

/**
 * @brief Rotate an array to the right by k positions, in-place.
 *
 * Elements shift toward the back; those that fall off wrap to the front.
 * Must use O(1) auxiliary space.
 *
 * @param arr  Pointer to the array
 * @param n    Number of elements
 * @param k    Number of positions to rotate right
 */
void rotate_right(int32_t *arr, int n, int k) {
    /* TODO: Implement your solution here */
}

