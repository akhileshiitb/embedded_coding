#include <stdint.h>

/**
 * @brief Swap the bits at two given positions in a 32-bit unsigned integer.
 *
 * @param num   The input 32-bit unsigned integer
 * @param pos1  First bit position to swap (0-indexed)
 * @param pos2  Second bit position to swap (0-indexed)
 * @return      The value with bits at pos1 and pos2 swapped
 */
uint32_t swap_bits(uint32_t num, int pos1, int pos2) {
    /* TODO: Implement your solution here */
    (void)num;
    (void)pos1;
    (void)pos2;
    return 0;
}
