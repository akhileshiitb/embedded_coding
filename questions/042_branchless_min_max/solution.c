#include <stdint.h>

/**
 * @brief Branchless minimum of two signed 32-bit integers.
 *
 * Must use only bitwise operations and comparison (no if/ternary).
 * Hint: create a conditional mask from the comparison result.
 *
 * @param x  First integer
 * @param y  Second integer
 * @return   The smaller of x and y
 */
int32_t branchless_min(int32_t x, int32_t y) {
    /* TODO: Implement your solution here */
    (void)x;
    (void)y;
    return 0;
}

/**
 * @brief Branchless maximum of two signed 32-bit integers.
 *
 * Must use only bitwise operations and comparison (no if/ternary).
 * Think about how min and max relate via XOR.
 *
 * @param x  First integer
 * @param y  Second integer
 * @return   The larger of x and y
 */
int32_t branchless_max(int32_t x, int32_t y) {
    /* TODO: Implement your solution here */
    (void)x;
    (void)y;
    return 0;
}
